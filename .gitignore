# Demo WebMs / captions are NEVER in git — not this tree, not the app repo.
# They live only on this repo's GitHub Release tag `demo-movies`.
# Refresh: cd ../solidexpress && make movies && make publish-demo-movies
assets/movies/
*.webm
*.mp4
*.avi
*.mov
*.vtt
